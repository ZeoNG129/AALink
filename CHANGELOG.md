# AALink 更新日志

## 1.1.5
- **修复：开关关闭期间放置的设备重进存档后又被自动连接** — 区块加载重连现在只恢复"曾经成功桥接过"的设备；`/aalink off` 后放置的设备从未桥接，重进存档/区块重载后保持断连

## 1.1.4
- **新增：自动连接开关** — 无线连接器默认关闭自动连接，开启后放置 AE 设备才会自动接入网络
- **新增：远程控制指令** — `/aalink on|off|toggle|status`（OP2 权限），远程开关自动连接
- **持久化开关** — 开关状态写入世界存档，服务器重启/重进后保持
- 关闭开关时**已建立的连接保持不断开**，重新开启后自动恢复连接
- 兼容 Forge 47.4.0 及以上版本

## 1.1.3
- 版本号推进，无独立变更记录

## 1.1.2
- 服务器重启后自动恢复连接（区块加载时重连）

## 1.1.1
- **修复：服务器重启断连** — 已放置的设备不再需要挖掉重放。区块重新加载时自动把设备节点重新桥接到中枢网络
- 时序保护：中枢未就绪时先记录设备，就绪后自动建连

## 1.1.0
- **修复：输出总线等 AE2 部件无法自动连接** — 部件（Part）不是完整方块，需通过 `IPartHost.getPart(side)` 取部件自身的节点才能桥接
- 优化：已知承载 AE 节点的方块实体类型缓存，同一类型方块只需查询一次，减少无关方块的 `getNodeHost` 调用

---

## 简介

AALink 让 AE2 设备无视距离、无视维度自动接入你的 ME 网络。把**无线连接器**放进 AE 网络，在任意位置放置任何能连接 AE 网络的方块，它们会自动接入——无需拉线、无需存储总线。

**前置**：Minecraft 1.20.1 + Forge 47.x + Applied Energistics 2（AE2）
